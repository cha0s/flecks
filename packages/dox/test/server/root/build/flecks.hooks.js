export const hooks = {
  /**
   * Description
   * @param {string} foo Foo
   * @param {number} bar Bar
   * @returns {Baz} Baz
   */
  '@something/blah': (foo, bar) => {
    this.doSomethingTo(foo, bar);
  },
};
